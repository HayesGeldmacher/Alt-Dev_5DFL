using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using using TMPro;

public class GunSafe : MonoBehaviour
{
    public TMP_Text _safeText;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
